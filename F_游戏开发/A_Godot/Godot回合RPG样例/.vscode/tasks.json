{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build", // launch.json中配置对应
            "type": "shell", // 在shell中启动
            // "command": "scons -j12 target=editor debug_symbols=yes", //执行命令
            "command": "scons", //执行命令
            "args": [ // 命令参数
                "-j12",
                "target=template_debug", //debug设置
                "debug_symbols=yes"//debug设置
            ],
            //"problemMatcher": "$msCompile"
        }
    ]
}